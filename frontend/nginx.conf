server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # SPA fallback - react-router handles /history, /predict client-side.
    location / {
        try_files $uri /index.html;
    }
}
