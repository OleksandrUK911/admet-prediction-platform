# optuna is a heavy, occasional-use dependency for
# ml/experiment_optuna_per_task.py (Optuna hyperparameter search for the
# per-task XGBoost classifiers) - kept out of the main requirements.txt/CI
# path, same isolation pattern as ml/requirements-multitask.txt (torch).
# Install with: pip install -r ml/requirements-optuna.txt
optuna==5.0.0
