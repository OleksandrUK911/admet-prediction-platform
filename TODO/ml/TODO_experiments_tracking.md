# TODO — Трекінг експериментів та вибір переможця (ADMET Prediction)

## Інфраструктура трекінгу
- [ ] Розгортання MLflow (локально або легкий hosted-варіант) або аналог (W&B) для логування експериментів — **P0** | ~3h
- [ ] Логування всіх експериментів (гіперпараметри, метрики per-task, версія даних) для відтворюваності — **P0** | ~2h
- [ ] Логування артефактів (ваги моделі, feature pipeline, конфіг) разом з кожним run'ом — **P1** | ~2h

## Порівняння підходів
- [ ] Зведена порівняльна таблиця per-task: baseline vs per-task tuned vs multi-task — **P0** | ~3h
- [ ] Порівняння за часом inference та розміром артефакту моделі (важливо для деплою) — **P1** | ~2h
- [ ] Візуалізація trade-off "якість метрик vs складність/вартість інференсу" — **P2** | ~2h

## Вибір фінальної моделі
- [ ] Формалізований критерій вибору переможця (напр. зважена сума PR-AUC per-task + calibration ECE) — **P0** | ~2h
- [ ] Фіксація обраної архітектури як "production" кандидата для `ml/TODO_model_registry.md` — **P0** | ~1h

### Примітки
- Рішення "multi-task чи per-task" має прийматися на основі зафіксованих у трекінгу цифр, а не апріорних очікувань — навіть якщо фінальний вибір виявиться простішим за початковий план (per-task), це нормальний і чесний результат для портфоліо.

### Залежності
- Агрегує результати з `ml/TODO_experiments_per_task_models.md` та `ml/TODO_experiments_multitask.md`. Обраний переможець передається далі в `ml/TODO_calibration_uncertainty.md` (калібрування фінальної моделі) та `ml/TODO_model_registry.md` (документація та версіонування).
