# TODO — Автентифікація та безпека (ADMET Prediction)

## Rate limiting
- [ ] Rate limiting на публічний ендпоінт `/admet-profile` (за IP або токеном) — **P1** | ~2h
- [ ] Захист від зловживання batch-запитами (обмеження кількості SMILES за раз) — **P1** | ~1h

## Валідація вводу
- [ ] Валідація коректності SMILES перед подачею в модель (RDKit parse check) — **P0** | ~1h
- [ ] Обмеження довжини/складності вхідного SMILES (захист від adversarial/дуже великих молекул) — **P1** | ~1h
- [ ] Санітизація вхідних даних проти injection-атак у запитах, що логуються в БД — **P0** | ~2h

## Запобігання неправильному сприйняттю сервісу
- [ ] Технічні заходи проти використання застосунку як "медичного" інструменту (обов'язковий дисклеймер у кожній відповіді API, не тільки в UI) — **P0** | ~1h
- [ ] Перевірка, що жодне поле відповіді API не використовує медичну термінологію без застереження (напр. "diagnosis", "safe to use") — **P0** | ~2h

### Примітки
- Це research-платформа без клінічної валідації — запобіжники проти "медичного" сприйняття мають бути на рівні API-контракту, а не лише фронтенду, оскільки API може використовуватись напряму.

### Залежності
- Дисклеймер-поле узгоджене зі схемою відповіді з `backend/TODO_api_design.md`. Наскрізна вимога "не для клінічного використання" тут дублюється на рівні `devops/TODO_security_compliance.md` як фінальна перевірка перед публічним запуском.
