# TODO — API Design (ADMET Prediction)

## Ендпоінти
- [ ] Ендпоінт `POST /admet-profile` (SMILES → повний ADMET-профіль) — **P0** | ~4h
- [ ] Ендпоінт `GET /admet-profile/{id}` для отримання збереженого результату за id запиту — **P1** | ~2h
- [ ] Ендпоінт `GET /health` для healthcheck (модель завантажена, БД доступна) — **P0** | ~1h

## Схема відповіді
- [ ] Повернення довірчого інтервалу/невизначеності разом з кожним передбаченням — **P0** | ~2h
- [ ] Позначка applicability domain у відповіді API (in-domain / out-of-domain прапорець) — **P0** | ~2h
- [ ] Явне поле-дисклеймер у кожній відповіді (research/educational use only) — **P0** | ~1h
- [ ] Версія моделі, що використовувалась для передбачення, у відповіді (для трасування) — **P0** | ~1h

## Контракт API
- [ ] OpenAPI/Swagger специфікація з описом усіх полів та прикладами — **P1** | ~2h
- [ ] Pydantic-схеми для request/response з валідацією типів — **P0** | ~2h
- [ ] API-документація та текст повідомлень про помилки — англійською мовою (продукт англомовний за замовчуванням) — **P1** | ~1h

### Примітки
- Перевикористати inference-каркас із проєкту №1, де можливо, щоб не дублювати FastAPI-boilerplate.

### Залежності
- Потребує фінальної моделі та її output-контракту з `ml/TODO_model_registry.md` (uncertainty, applicability domain, версія). Дисклеймер-поле тут узгоджене з `backend/TODO_auth_security.md` та `frontend/TODO_ui_components.md`.
