# TODO — Пайплайн препроцесингу (ADMET Prediction)

## Канонізація SMILES
- [ ] Уніфікація SMILES між різними датасетами (RDKit-канонізація, однаковий алгоритм для всіх джерел) — **P0** | ~3h
- [ ] Нормалізація солей/фрагментів (salt stripping, вибір головного фрагменту молекули) — **P0** | ~2h
- [ ] Обробка стереохімії (рішення: враховувати stereo-центри чи ігнорувати для спрощення задачі) — **P1** | ~2h
- [ ] Видалення молекул, які RDKit не може розпарсити (з логуванням причин) — **P0** | ~1h

## Побудова multi-task таблиці
- [ ] Побудова єдиної multi-task таблиці (молекула → набір ADMET-міток з усіх датасетів) — **P0** | ~4h
- [ ] Merge за канонічним SMILES з дедуплікацією однакових молекул із різних джерел — **P0** | ~3h
- [ ] Обробка пропущених значень (не всі молекули мають усі мітки — маскування NaN замість imputation для класифікаційних задач) — **P0** | ~2h
- [ ] Генерація фінального parquet/csv артефакту з фіксованою схемою колонок (SMILES, task_1..task_n, split) — **P0** | ~2h

### Примітки
- Merge даних з різних джерел за SMILES — джерело прихованих помилок (різні канонізації дають різний рядок для однієї молекули), тому канонізація має відбуватись ДО merge, одним і тим самим кодом для всіх датасетів.

### Залежності
- Потребує завершеного вибору датасетів з `data/TODO_sources_licensing.md`. Фінальний артефакт (multi-task таблиця) — вхідні дані для `data/TODO_quality_validation.md` (split, дедуплікація) та для всіх файлів `ml/` (baseline, feature engineering, experiments).
