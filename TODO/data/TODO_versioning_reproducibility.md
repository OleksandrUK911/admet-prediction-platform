# TODO — Версіонування та відтворюваність (ADMET Prediction)

## Версіонування датасетів
- [ ] Фіксація версій вихідних датасетів (версія/дата завантаження TDC/MoleculeNet) — **P0** | ~1h
- [ ] Збереження raw-копій датасетів окремо від оброблених (data/raw vs data/processed) — **P0** | ~1h
- [ ] DVC або аналог для версіонування великих файлів (за потреби) — **P2** | ~3h

## Відтворюваність
- [ ] Реалізація reproducible-скрипту генерації multi-task таблиці "одна команда — один результат" — **P0** | ~3h
- [ ] Фіксація random seed для всіх splits та sampling-операцій — **P0** | ~1h
- [ ] Changelog для змін у пайплайні препроцесингу з поясненням впливу на дані — **P2** | ~1h

### Примітки
- Скрипт генерації датасету має бути ідемпотентним і детермінованим — це необхідна умова для того, щоб CI міг автоматично перевіряти каліброваність моделі при зміні даних (див. DevOps TODO).

### Залежності
- Детермінований пайплайн тут — передумова для CI-перевірок калібрування в `devops/TODO_cicd.md` ("Перевірки, специфічні для ML"). Без фіксованого seed тут метрики в `ml/TODO_evaluation_validation.md` не будуть відтворюваними.
